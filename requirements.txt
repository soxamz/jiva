# Production dependencies for Vercel's Python runtime. Local-only tooling
# (uvicorn, python-dotenv, and the Windows-only pywin32 package) stays in
# api/requirements.txt and is not bundled into the serverless function.
fastapi>=0.115.0
python-multipart>=0.0.12
pydantic>=2.11.9,<2.13
pydantic-settings>=2.6.0
groq>=0.11.0
google-genai>=1.0.0
